name: Test
# comment
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set destination branch - Push or merge to master
      if: (github.event.inputs.dest_env == '' && ${{github.ref_name == 'release/*'}})
      run: echo "main"
